The files provided MUST use typedef struct {} s; syntax instead of struct s {}; .
"->" will be treated as "." .
Now supports mulitple atributte definitions in one sentence.
Now supports globalAttributes, but still dont show them.
Now supports reading more than 1 cipher numbers.
Now supports reading characters written between  '  ' .
Now supports functions that return structs.
